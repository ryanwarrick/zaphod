{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Set ENV and Init DB",
            "type": "shell",
            // "command": "$env:FLASK_APP = 'zaphod'; flask init-db;"
            "command": "${workspaceFolder}${pathSeparator}venv${pathSeparator}Scripts${pathSeparator}activate; $env:FLASK_APP = 'zaphod'; flask init-db"
        }
    ]
}